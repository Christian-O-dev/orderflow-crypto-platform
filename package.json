{
  "name": "orderflow-crypto-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "npm run build -w packages/shared && concurrently \"npm --ignore-scripts run dev -w apps/backend\" \"npm --ignore-scripts run dev -w apps/frontend\"",
    "dev:frontend": "npm run dev -w apps/frontend",
    "dev:backend": "npm run dev -w apps/backend",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
